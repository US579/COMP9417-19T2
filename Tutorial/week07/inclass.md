## Duality : dual view of percepton + SVM

## kernel trick : how to deal with the non-seprateable data
	* 将数据映射到HIGH DIMENSION
	* 线性不可分变成线性可分


## when use duel view?
- only know the dot product x_i * x

# SVM

1/2||w||^2 == 1/2 w^T * w